apiVersion: v1
kind: Service
metadata:
  name: open-webui-service
  namespace: ia-llm
spec:
  selector:
    app: open-webui
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081
